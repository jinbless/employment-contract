services:
  contract:
    image: employment-contract
    build:
      context: .
      dockerfile: Dockerfile
    container_name: employment-contract
    restart: unless-stopped
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
    volumes:
      - ./server/.env:/app/server/.env:ro
